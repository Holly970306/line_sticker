---
description: Initialize the LINE Sticker Maker project
---

This workflow will set up the environment and ensure all dependencies are installed.

// turbo
1. Install dependencies:
`npm install`

2. Check the project structure:
`ls src`

3. Start the development server (optional):
`npm run dev`
